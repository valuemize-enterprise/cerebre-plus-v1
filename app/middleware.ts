// ═══════════════════════════════════════════════════════════════
// /app/middleware.ts — DEAD CODE — DO NOT USE
//
// Next.js only recognises middleware.ts at the PROJECT ROOT
// (same level as package.json). A file at app/middleware.ts
// is treated as a regular module — it is NEVER run by the
// Next.js Edge runtime.
//
// The live middleware is at /middleware.ts (project root).
//
// This file is kept for historical reference only. It is safe
// to delete entirely. If you need to add middleware logic,
// edit /middleware.ts instead.
// ═══════════════════════════════════════════════════════════════

export {}
